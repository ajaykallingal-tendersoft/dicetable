<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.SoloSeaters" parent="@android:style/Theme.Light.NoTitleBar">
        <!-- Modern Android 12+ splash screen configuration -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@null</item>
        <!-- item name="android:postSplashScreenTheme">@style/Theme.SoloSeaters</item -->
    </style>
</resources>